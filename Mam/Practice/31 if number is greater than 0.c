#include<stdio.h>
int main()
{
        int number;
        printf("Enter an integer");
        scanf("%d",&number);

        if (number > 0)
        printf("The number you entered is greater than 0");
        else
        printf("The number you entered is less than 0");
        }
